version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres_production
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${POSTGRES_DB:-hotinbox}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--auth-host=scram-sha-256"
      # Remove this line: PGDATA: /var/lib/postgresql/data/pgdata

    volumes:
      - postgres_data:/var/lib/postgresql/data

    ports:
      - "5432:5432"

    command: >
      postgres
      -c listen_addresses='*'
      -c max_connections=200
      -c superuser_reserved_connections=3
      -c password_encryption=scram-sha-256
      -c shared_buffers=256MB
      -c effective_cache_size=1GB
      -c work_mem=4MB
      -c maintenance_work_mem=64MB
      -c wal_buffers=16MB
      -c checkpoint_completion_target=0.9
      -c wal_level=replica
      -c max_wal_size=1GB
      -c min_wal_size=80MB
      -c checkpoint_timeout=5min
      -c log_destination=stderr
      -c log_min_messages=warning
      -c log_min_error_statement=error
      -c log_min_duration_statement=1000
      -c log_statement=mod
      -c shared_preload_libraries='pg_stat_statements'
      -c random_page_cost=1.1
      -c effective_io_concurrency=200
      -c default_statistics_target=100
      -c autovacuum=on
      -c autovacuum_max_workers=3
      -c autovacuum_naptime=1min
      -c ssl=off
      -c timezone='UTC'
      -c log_timezone='UTC'

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-hotinbox}",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"

    networks:
      - postgres_network

    user: postgres

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/storage/postgres-data

networks:
  postgres_network:
    driver: bridge
